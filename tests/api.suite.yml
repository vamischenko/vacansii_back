actor: ApiTester
modules:
    enabled:
        - REST:
            url: /
            depends: Yii2
        - Yii2:
            part: [init, orm, fixtures, email]
            configFile: 'config/test.php'
            entryScript: index-test.php
            cleanup: true
        - Asserts
